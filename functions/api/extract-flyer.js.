// /functions/api/extract-flyer.js
// TEMPORARY diagnostic version, stripped down to almost nothing, to isolate
// whether the 405 issue is about this filename specifically, or about the
// real content of this file. Once we know, we'll restore the real version.

export async function onRequestPost(context) {
  return new Response(JSON.stringify({ ok: true, message: 'Bare-bones extract-flyer.js reached.' }), {
    headers: { 'Content-Type': 'application/json' }
  });
}
